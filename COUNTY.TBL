DROP TABLE GENSYS.COUNTY;

CREATE TABLE GENSYS.COUNTY
    (
    STATE_CODE          CHAR(2) NOT NULL,
    COUNTY_CODE         CHAR(3) NOT NULL,
    COUNTY_NAME         VARCHAR(65) NOT NULL,

    PRIMARY KEY(STATE_CODE, COUNTY_CODE));