/* STATES TABLE */

DROP TABLE GENSYS.STATES;

CREATE TABLE GENSYS.STATES
    (
    STATE_CODE          CHAR(2) NOT NULL,
    STATE_NAME          VARCHAR(25) NOT NULL,
    COUNTRY             VARCHAR(25),

    PRIMARY KEY(STATE_CODE));